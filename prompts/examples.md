GitHub is available via the `gh` namespace — issues, pull requests, repos, commits, users, releases. Activate the **`github-workflows`** skill via the skill tool before making any GitHub call; the skill carries the namespace conventions, search-then-get patterns, pagination, and rate-limit handling.
